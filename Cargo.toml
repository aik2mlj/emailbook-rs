[package]
name = "emailbook"
version = "0.1.3"
edition = "2024"
description = "A minimalistic address book for e-mails only"
license = "MIT"

[dependencies]
base64 = "0.22"
clap = { version = "4.5.60", features = ["derive"] }
encoding_rs = "0.8"
regex = "1"

[dev-dependencies]
tempfile = "3"
